

services:
  backend:
    build: ./backend                # path to backend Dockerfile
    container_name: cookswipe-backend
    ports:
      - "5001:5000"                 # host:container
    env_file:
      - ./backend/.env              # Supabase keys, etc.
    volumes:
      - ./backend:/app              # live-reload during dev
    restart: always
